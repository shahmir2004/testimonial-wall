// src/pages/LoginPage.jsx
import React from 'react';

function LoginPage() {
  // We will add Supabase auth UI or a manual form here in Phase 2
  return (
    <div>
      <h2>Login / Sign Up</h2>
      <p>The user authentication form will go here.</p>
      <p>We'll use Supabase Auth to handle logins, sign-ups, and magic links.</p>
    </div>
  );
}

export default LoginPage;